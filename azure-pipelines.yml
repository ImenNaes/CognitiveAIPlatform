# Docker
# Build and push an image to Azure Container Registry
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
- master

resources:
- repo: self

variables:
  # Container registry service connection established during pipeline creation
  dockerRegistryServiceConnection: 'e9961d58-8fea-4b13-9f61-88a44ea7ed96'
  imageRepository: 'imennaescognitiveaiplatform'
  containerRegistry: 'cogaiplatformregistry.azurecr.io'
  dockerfilePath: '$(Build.SourcesDirectory)/Dockerfile'
  tag: '$(Build.BuildId)'
  ResourceGroup: 'rg-cogai-platform'
  devContainerApp: 'cogai-platform-dev'
  prodContainerApp: 'cogai-platform-prod'

  # Agent VM image name
  vmImageName: 'ubuntu-latest'

stages:
- stage: Build
  displayName: Build and push stage
  jobs:
  - job: Build
    displayName: Build
    pool:
      vmImage: $(vmImageName)
    steps:
    - task: Docker@2
      displayName: Build and push an image to container registry
      inputs:
        command: buildAndPush
        repository: $(imageRepository)
        dockerfile: $(dockerfilePath)
        containerRegistry: $(dockerRegistryServiceConnection)
        tags: |
          $(tag)

# =========================
# 2. Deploy Dev
# =========================
- stage: Deploy_Dev
  displayName: Deploy to Dev
  dependsOn: Build
  jobs:
  - job: DeployDev
    displayName: Deploy to Azure Container Apps (Dev)
    steps:
    - task: AzureCLI@2
      displayName: Deploy to Dev
      inputs:
        azureSubscription: 'azure-connection-service'
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          az extension add --name containerapp --yes

          az containerapp update \
            --name $(devContainerApp) \
            --resource-group $(ResourceGroup) \
            --image $(containerRegistry)/$(imageRepository):$(Build.BuildId) \
            --set-env-vars ASPNETCORE_ENVIRONMENT=Development

# =========================
# 3. Deploy Prod
# =========================
- stage: Deploy_Prod
  displayName: Deploy to Prod
  dependsOn: Deploy_Dev
  condition: succeeded()
  jobs:
  - job: DeployProd
    displayName: Deploy to Azure Container Apps (Prod)
    pool:
      vmImage: $(vmImageName)
    steps:
    - task: AzureCLI@2
      displayName: Deploy to Prod
      inputs:
        azureSubscription: 'azure-connection-service'
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          az extension add --name containerapp --yes

          az containerapp update \
            --name $(prodContainerApp) \
            --resource-group $(ResourceGroup) \
            --image $(containerRegistry)/$(imageRepository):$(Build.BuildId) \
            --set-env-vars ASPNETCORE_ENVIRONMENT=Production
